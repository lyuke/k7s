## 改进计划

### 1. 右侧集群展示改为卡片式布局
- 修改 App.css：
  - 添加 `.cluster-grid` 网格布局样式（responsive，2-3列）
  - 添加 `.cluster-card` 卡片样式（圆角、阴影、悬停效果）
  - 添加卡片状态指示器（颜色标识连接状态）
  - 添加节点/Pod 统计信息展示区域

- 修改 App.tsx：
  - 新增 `ClusterCard` 组件
  - 将列表渲染改为网格布局
  - 添加集群概览统计（节点数、Pod数）

### 2. 修复添加接口
- 修改 kube.ts：
  - 改进 `buildId` 逻辑，使用文件路径+context名称避免冲突
  - 添加文件读取错误处理
  - 添加重复 context 检测

- 修改 index.ts（main）：
  - 添加添加过程中的错误捕获
  - 返回更详细的错误信息给前端

- 修改 App.tsx：
  - 添加添加过程中的 loading 状态
  - 添加错误提示 toast/notification
  - 添加成功反馈

### 3. 额外优化
- 添加空状态引导（首次使用提示添加集群）
- 添加集群卡片点击后的详情展开/收起
- 优化移动端适配