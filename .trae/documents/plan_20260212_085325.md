## 实现计划

### 1. 改进命名空间筛选功能
- 当前命名空间筛选已存在，但需要优化交互
- 添加命名空间搜索功能（当命名空间很多时方便查找）
- 改进下拉框样式

### 2. 资源列表添加滚动
- 修改 `.resource-section` 和 `.table` 的 CSS 样式
- 设置表格区域 `overflow-y: auto` 和最大高度
- 保持表头固定，内容可滚动

### 3. 添加定时刷新机制
- 添加刷新间隔设置（默认 30 秒）
- 使用 `useEffect` + `setInterval` 实现定时刷新
- 在工具栏显示刷新状态和手动刷新按钮
- 切换集群或命名空间时重置定时器

### 文件修改
1. `src/renderer/src/App.tsx` - 添加定时刷新逻辑和刷新按钮
2. `src/renderer/src/App.css` - 添加表格滚动样式